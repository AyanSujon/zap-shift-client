import React from 'react';
import logo from '../../assets/logo.png'

const Logo = () => {
    return (
        <div>
            <img src={logo} alt="Site Logo" />
        </div>
    );
};

export default Logo;